# --- AI Provider Override (Optional) ---
# Set these to explicitly choose a provider and model.
# If not set, ArcadeForge auto-detects from available API keys.
# ARCADEFORGE_PROVIDER=google    # google | openai | anthropic | ollama
# ARCADEFORGE_MODEL=gemini-2.0-flash

# --- AI API Keys ---
# At least one is required for Creative Partner and AI config generation.
# You can also configure these via: arcadeforge config
GOOGLE_API_KEY=your_gemini_api_key
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# --- Asset Generation ---
STABILITY_API_KEY=your_stability_ai_key

# --- Supabase Configuration (Required for Hub) ---
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# --- Local Development ---
ARCADEFORGE_HUB_URL=http://localhost:3000/api/submit
